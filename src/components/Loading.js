import React from 'react';

const Loading = () => {
    return (
        <div className=' h-screen flex items-center justify-center'>
            <h1 className='text-center text-red-600 text-xl'>Loading...</h1>
        </div>
    );
};

export default Loading;